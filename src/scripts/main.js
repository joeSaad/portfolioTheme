    $(".fancybox").fancybox({
    	padding: 0,
    	helpers: {
    		overlay: {
      			locked: false
    		}
  		}
    });

   /* if ($('li.project').length > 1) {
        $('li.project:gt(0)').hide();
        $('.fa-ellipsis-v').show();
    };*/
